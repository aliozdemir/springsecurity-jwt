spring:
  datasource:
    url : jdbc:mysql://localhost:3306/demo?useUnicode=true&useLegacyDatetimeCode=false&serverTimezone=Turkey
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect : org.hibernate.dialect.MySQL5Dialect